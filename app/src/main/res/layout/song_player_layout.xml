<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_gravity="center"
    android:background="#ffffff"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="7dp"
    android:weightSum="100">

    <TextView
        android:id="@+id/tv_current_song_name"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="10"
        android:gravity="center"
        android:text="Current Song Name"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:textColor="@color/colorPrimaryDark" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="20"
        android:gravity="center"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tv_previous_song"
            style="@style/song_player_buttons"
            android:text="prev" />

        <TextView
            android:id="@+id/tv_stop_song"
            style="@style/song_player_buttons"
            android:text="||" />

        <TextView
            android:id="@+id/tv_next_song"
            style="@style/song_player_buttons"
            android:text="next" />
    </LinearLayout>

    <ProgressBar
        android:id="@+id/pb_song_progress_percent"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="5"
        android:background="@color/colorAccent" />
</LinearLayout>
